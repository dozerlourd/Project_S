using UnityEngine;

namespace ProjectS.Units
{
    public sealed class UnitTeamIndicator : MonoBehaviour
    {
        // Legacy prefab compatibility only. Team identity is no longer conveyed by a color marker.
    }
}
